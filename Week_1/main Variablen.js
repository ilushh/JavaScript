// Kontakformulare
"use strict"; 

//alert("Hi Serife.");

/* mit prompt kann man ein PopUp Fenster erzeugen wo auch der NAme derin steht
prompt("Wie ist dein Name?", "Ilushh");

//Variable (den richtigen Namen) in name speichern
let name = prompt("Wie ist dein Name?");
console.log(name); 
--------------------------------------------------*/

/*variable deklarieren heißt einem Programm erklkären dass es so ein Programmm gibt. let stadt
initialisieren von variablen durch wert zuweisung let alter = 24 */

let stadt  = "Dortmund"; //Keyword, mit der wir eine Variable definieren.
let alter = 24; // 24 der variable zuweisen. Variablen sind Speicherorte. 

console.log(stadt);
console.log(alter); // um in der Colsone es auszugeben.
//-------------------------------------------------------


// primitive Datentypen string number boolean (ohne Anführungszeichen), Punkt verwenden kein Komma

let ich_mag_serife = true;
let ich_mag_dill = false;
console.log(ich_mag_dill);


// Constante sind Werte die sich nicht mehr verändern sollen const, Wert der konstante ist geschüzt. Const müssen auch immer initialisiert werden.
const car = {type:"VW", model: 500, color: "white"};


